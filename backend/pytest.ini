[pytest]
testpaths = tests
pythonpath = . ..
addopts = -ra --strict-markers --tb=short
markers =
    ml: machine-learning tests that need torch
    slow: slow tests (training / full evaluation)
filterwarnings =
    ignore::DeprecationWarning:passlib.*
